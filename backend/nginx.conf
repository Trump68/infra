server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;

    default_type text/plain;

    location / {
        return 200 'OK';
    }

    location /api {
        return 200 'OK';
    }

    location /api/ {
        return 200 'OK';
    }
}
